[tool.ruff]
line-length = 100

target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]

ignore = []

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
addopts = "--cov=src --cov-fail-under=1 --cov-report=term-missing"
testpaths = ["test"]
pythonpath = [".", "src"]